CREATE TABLE mp3data ( filename text, filesize text,mimetype text, filetypeextension text,comment text, album   text,     composer text, genre text,
warning text, created_date   date,      grouping text, title   text, artist  text,  duration text );
